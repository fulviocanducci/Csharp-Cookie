@{Layout = null;}

@if (User != null && User.Identity != null && User.Identity.IsAuthenticated)
{
    <form asp-action="End" method="post" asp-controller="Login">
        <button type="submit" class="btn btn-primary btn-sm">Sair, @User.Identity.Name</button>
    </form>
}